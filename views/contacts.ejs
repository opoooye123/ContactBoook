<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Contacts</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #f9fafc;
            margin: 0;
            padding: 20px;
        }

        h3 {
            text-align: center;
            font-size: 28px;
            color: #333;
            margin-bottom: 30px;
        }

        .contact-card {
            background: #fff;
            padding: 20px;
            margin: 15px auto;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.05);
            max-width: 600px;
            transition: transform 0.2s ease;
        }

        .contact-card:hover {
            transform: translateY(-4px);
        }

        ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        li {
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }

        li:last-child {
            border-bottom: none;
        }

        p {
            margin: 0;
            font-size: 16px;
            color: #444;
        }

        .label {
            font-weight: bold;
            color: #222;
        }
    </style>
</head>

<body>

    <h3>Contacts</h3>

    <% for(let info of information) { %>
        <div class="contact-card">
            <ul>
                <li>
                    <p><span class="label">Contact Name:</span>
                        <%= info.names %>
                    </p>
                </li>
                <li>
                    <p><span class="label">Contact Number:</span>
                        <%= info.contact %>
                    </p>
                </li>
                <li>
                    <p><span class="label">Country:</span>
                        <%= info.country %>
                    </p>
                </li>
            </ul>
            <a href="/contacts/<%= info.id %>">Details</a>
        </div>
        <% } %>
            <a href="/contacts/new">Add Contact</a>
</body>

</html>